# url-shortener
URL shortener built with Spring Boot that generates random slugs (5–10 characters), supports automatic expiration (24h), logs detailed click metadata (IP, User-Agent, Referer, Accept-Language), and provides full URL analytics through a structured REST API.
